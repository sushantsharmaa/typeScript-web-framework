{
  "dependencies": {
    "axios": "^1.6.8",
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "npx json-server -w db.json"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
